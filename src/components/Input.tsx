export { Input as default } from "@/components/ui/input";
/**
 * Input (re-export)
 * Purpose: Re-export shadcn Input so local components can import from a stable path.
 * Usage: import Input from '@/components/Input'
 */
